export const arrayOfChores = state => {
  console.log("STATE", state)
  if (state) {
    let userChoresObj
    let arrayOfChores = state.chores
    let arrayOfChildren = state.child.children
    let finalArr = []
    console.log("HELPER", state)
  }
}

